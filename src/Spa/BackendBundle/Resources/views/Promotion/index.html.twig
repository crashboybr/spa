{% extends '::base.html.twig' %}

{% block body -%}
   <h1>Promoções</h1>
    <ul class="botoes">
        <li><a href="spa-cms-promo-novo.html" class="botaoCriar"><i class="icon-plus"></i> Criar nova Promoção</a></li>
    </ul>
    <table class="records_list">
        <thead>
            <tr>
                <th>Ações</th>
                <th>Título</th>
                <th>Imagem</th>
                <th>Link</th>
                <th>Fixa</th>
            </tr>
        </thead>
        <tbody>
                        
        {% for entity in entities %}
            <tr>
                <td>
                    <ul class="ordenacao">
                        <li><i class="icon-arrow-up disabled"></i></li>
                        <li><i class="icon-arrow-down disabled"></i></li>
                        <li><i class="icon-eye-open"></i></li>
                        <li><a href="{{ path('promocao_edit', { 'id': entity.id }) }}" title="editar"><i class="icon-edit"></i></a></li>
                        <li><i class="icon-trash disabled"></i></li></ul>
                </td>
                <td><a href="{{ path('promocao_edit', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td><img src="{{ asset(entity.pic) }}" style="width: 98px" /></td>
                <td>{{ entity.slug }}</td>
                <td>{{ entity.fixed }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <ul class="botoes">
        <li><a href="spa-cms-promo-novo.html" class="botaoCriar"><i class="icon-plus"></i> Criar nova Promoção</a></li>
    </ul>
    {% endblock %}
